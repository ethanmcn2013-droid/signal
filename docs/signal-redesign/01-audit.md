# Audit and reconciled truth

The audit found a split contract: older product copy describes four blocks, the current builder caps distinct candidates at three, and web currently hides two legacy blocks while email still renders them. The lab therefore uses one frozen three-claim truth model and labels Moving well / Quiet risks as an experiment.

Confirmed: Signal is sentence-first, Tasks is the shipped source, raw Notes bodies are excluded, and `/app` remains the production Briefing route. Unknown or simulated: Timeline confidence, persisted history, guest receipts, and task creation.
