# Accessibility contract

The lab provides a skip link, one main landmark, ordered headings, native fieldsets/selects, explicit status announcements, minimum target sizing, native modal dialogs, keyboard Escape handling, focus return, reduced-motion CSS, and a linear mobile fallback for the Lens. Browser verification must still include axe/NVDA or VoiceOver before selection.
