# Signal attention briefing design lab

Phase 1 is a fixture-only, review-only comparison of three coded information architectures. It does not replace `/app`, write production data, or imply that simulated Timeline, history, receipts, or Signal-to-Task behavior has shipped.

The local route is `/__design-lab/signal`. Preview access requires review mode and `SIGNAL_DESIGN_LAB_ENABLED=true`.
